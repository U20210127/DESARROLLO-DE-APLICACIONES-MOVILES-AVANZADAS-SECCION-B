<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:background="@android:color/white"
        android:gravity="center">

        <!-- Botón para tomar fotografía -->
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/btnCapture"
            android:text="Tomar Fotografía"
            android:textColor="@android:color/white"
            android:backgroundTint="#6200EE"
            android:layout_marginBottom="12dp"
            android:layout_gravity="center"
            android:textSize="16sp"
            android:padding="12dp"
            app:cornerRadius="8dp"/>

        <!-- Botón para compartir foto -->
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/btnShare"
            android:text="Compartir Foto"
            android:textColor="@android:color/white"
            android:backgroundTint="#03DAC5"
            android:layout_marginBottom="12dp"
            android:textSize="16sp"
            android:padding="12dp"
            app:cornerRadius="8dp"/>

        <!-- Botón para borrar foto -->
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/btnDelete"
            android:text="Borrar Foto"
            android:textColor="@android:color/white"
            android:backgroundTint="#B00020"
            android:layout_marginBottom="24dp"
            android:textSize="16sp"
            android:padding="12dp"
            app:cornerRadius="8dp"/>

        <!-- CardView con la vista previa de la imagen -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            app:cardCornerRadius="12dp"
            app:cardElevation="6dp">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="300dp"
                android:id="@+id/imgPreview"
                android:scaleType="centerCrop"/>
        </androidx.cardview.widget.CardView>

        <!-- Texto para mostrar metadatos -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:id="@+id/textViewMetadata"
            android:textColor="@android:color/black"
            android:textSize="14sp"
            android:gravity="center"
            android:background="@drawable/metadata_background"
            android:text="Metadatos de la foto"/>

    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>

